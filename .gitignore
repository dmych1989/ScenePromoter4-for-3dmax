# ===== 构建产物 / 二进制安装包（保持代码纯净，不纳入版本库）=====
*.exe
dist/
build/
_sp4_stage/
payload.zip
*.spec

# 缓存文件
._cache_*

# 由 logo.gif 自动生成的图标（构建产物）
images/logo.ico

# 本地工作数据（不发布）
.workbuddy/

# 操作系统 / 编辑器
Thumbs.db
.DS_Store
*.suo
*.user

# Windows 保留设备名(nul)伪文件：Git Bash 中 `2>nul` 会误建此 0 字节文件，
# 无法用常规方式删除，且 `git add -A` 会因设备名报错，故忽略之。
nul
